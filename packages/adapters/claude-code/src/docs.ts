import type { AdapterDocs } from '@driftgate/adapter-kit';

const CLAUDE_MEMORY_DOCS = {
  url: 'https://docs.claude.com/en/docs/claude-code/memory',
  title: 'Claude Code — Manage Claude’s memory',
  retrieved: '2026-09-01',
} as const;

/**
 * Encoded precedence knowledge for Claude Code.
 *
 * This is the moat: incumbents already generate files, but nobody has written down
 * what each tool actually loads and in what order. Every claim here carries a source
 * link and the version it was checked against, so a reviewer can verify it and a stale
 * entry is visible rather than silently wrong.
 */
export const docs: AdapterDocs = {
  toolName: 'Claude Code',
  homepage: 'https://docs.claude.com/en/docs/claude-code',
  verifiedAgainst: { version: '2.x', date: '2026-09-01' },
  files: [
    {
      pattern: 'CLAUDE.local.md',
      scope: 'project',
      role: 'instructions',
      managed: false,
      description:
        'Personal project-local overrides. Deprecated in favour of imports, and never generated by Driftgate — it is by definition not shared.',
      source: CLAUDE_MEMORY_DOCS,
    },
    {
      pattern: 'CLAUDE.md',
      scope: 'project',
      role: 'instructions',
      managed: true,
      nesting: 'nearest-wins',
      description:
        'Project memory, committed to the repository. The file Driftgate generates. Claude Code also reads CLAUDE.md files in subdirectories when it works on files beneath them.',
      source: CLAUDE_MEMORY_DOCS,
    },
    {
      pattern: '~/.claude/CLAUDE.md',
      scope: 'global',
      role: 'instructions',
      managed: false,
      description:
        'User-level memory applied across every project. Read-only context for `doctor`: Driftgate never writes outside the repository.',
      source: CLAUDE_MEMORY_DOCS,
    },
    {
      pattern: '.claude/settings.json',
      scope: 'project',
      role: 'settings',
      managed: false,
      description:
        'Project settings — permissions, hooks, model. Detected but not generated; see the note below.',
      source: {
        url: 'https://docs.claude.com/en/docs/claude-code/settings',
        title: 'Claude Code — Settings',
        retrieved: '2026-09-01',
      },
    },
  ],
  notes: [
    {
      level: 'info',
      message:
        'Claude Code has no native per-glob rule scoping, so a glob-scoped canonical rule is rendered with an "Applies to:" line stating its scope in prose. This mapping is lossy but visible; dropping the scope silently would turn a component-only rule into a repo-wide one.',
    },
    {
      level: 'info',
      message:
        'Driftgate writes CLAUDE.md only. `.claude/settings.json` carries permissions and hooks — a materially different trust surface from instructions — and generating it is deliberately out of scope for v0.',
    },
    {
      level: 'warn',
      message:
        'Claude Code does not read AGENTS.md natively. That gap is the reason this project exists; the generated CLAUDE.md is what closes it.',
      source: {
        url: 'https://github.com/anthropics/claude-code/issues/6235',
        title: 'claude-code#6235 — Support AGENTS.md',
        retrieved: '2026-09-01',
      },
    },
  ],
};
