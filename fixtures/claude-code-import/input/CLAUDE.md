<!-- generated by driftgate; edit .driftgate/ instead -->

This project is a payments gateway. Read `docs/architecture.md` before changing anything
under `src/ledger/`.

## Style

Use tabs. Never `any`.

A fenced block that contains a heading, which must not split the section:

```md
## Not a heading

still the style rule
```

## Frontend

**Applies to:** `src/components/**/*.tsx`, `src/app/**/*.tsx`

Prefer server components.

## 日本語の見出し

Write commit messages in English.
