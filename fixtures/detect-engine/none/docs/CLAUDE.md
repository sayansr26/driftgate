# Notes on our Claude usage

A `CLAUDE.md` that is not at the repository root. Claude Code does read nested memory
files, but detection asks a different question — is this repository *configured* for the
tool — and the answer here is no.
