{
  "name": "@rulegate/action",
  "version": "0.1.0",
  "description": "GitHub Action wrapper for `rulegate check`.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node build.mjs",
    "build:check": "node build.mjs --check"
  },
  "dependencies": {
    "rulegate": "workspace:*"
  }
}
